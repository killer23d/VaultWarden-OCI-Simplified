# docker-compose.override.yml - Local environment overrides
# This file allows customization without modifying the main docker-compose.yml
# Copy this template and uncomment/modify sections as needed

version: "3.9"

services:
  # VaultWarden service overrides
  # vaultwarden:
  #   # Development/testing overrides
  #   environment:
  #     - LOG_LEVEL=debug
  #     - EXTENDED_LOGGING=true
  #   # Custom volume mounts
  #   volumes:
  #     - ./custom-config:/custom:ro
  #   # Port exposure for development
  #   ports:
  #     - "8080:8080"  # Direct access to VaultWarden
  #     - "3012:3012"  # WebSocket port

  # Caddy service overrides  
  # caddy:
  #   # Development/testing overrides
  #   ports:
  #     - "8081:80"   # Alternative HTTP port
  #     - "8443:443"  # Alternative HTTPS port
  #   # Custom Caddyfile for development
  #   volumes:
  #     - ./caddy/Caddyfile.dev:/etc/caddy/Caddyfile:ro

  # Fail2ban service overrides
  # fail2ban:
  #   # Less restrictive settings for development
  #   environment:
  #     - VERBOSITY=-vv  # More verbose logging
  #   # Custom configuration
  #   volumes:
  #     - ./fail2ban/jail.dev.conf:/config/jail.d/vaultwarden.conf:ro

  # Additional development services
  # adminer:
  #   image: adminer:4.8.1
  #   container_name: vaultwarden_adminer
  #   restart: unless-stopped
  #   ports:
  #     - "8082:8080"
  #   environment:
  #     - ADMINER_DEFAULT_SERVER=sqlite
  #   volumes:
  #     - /var/lib/vaultwarden/data/bwdata:/data:ro

  # Monitoring service (Prometheus/Grafana example)
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: vaultwarden_prometheus
  #   restart: unless-stopped
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #     - prometheus_data:/prometheus
  #   command:
  #     - '--config.file=/etc/prometheus/prometheus.yml'
  #     - '--storage.tsdb.path=/prometheus'

# Additional volumes for override services
# volumes:
#   prometheus_data:

# Additional networks if needed
# networks:
#   monitoring:
#     external: true
